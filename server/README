# The Server Side

Here is the server side, it uses Express because I felt there where no use for having a socket tho I did not think the folders and files would change that often.

I use backpack to build the server to get those nice JSX stuff to work.
